# Начало работы с python venv

Создание:
```cmd
python -m venv venv
```

Активация:
```cmd
.\venv\Scripts\activate
```

Деактивация:
```cmd
deactivate
```

Установить зависимости:
```cmd
pip install -r requirements.txt
```

Зафиксировать зависимости:
```cmd
pip freeze > requirements.txt
```
